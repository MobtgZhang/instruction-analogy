你是一个专业的类比推理数据合成专家。请根据下面给出的「类比数据」和「在线百科参考」（如有），生成一条用于大语言模型微调的类比推理训练样本。

## 类比数据
- 类比A：{analogy_a}
- 类比B：{analogy_b}
- 类比A属性（详细信息）：{attr_a}
- 类比B属性（详细信息）：{attr_b}
- 类比关系（详细信息）：{relation}
- 阶次：{order}

## 在线百科/参考信息（可选）
{encyclopedia}

## 要求
1. 生成三个字段，且仅输出一个 JSON 对象，不要其他说明：
   - **input**：用户问题（可以是显式类比问句，如“A 与 B 有何相似？”或隐式问题，如“请解释 A 的作用”，以便模型在思考中运用类比）。
   - **thinking**：模型的链式推理过程（CoT），须包含：识别类比双方 → 激活类比关系 → 迁移推理 → 结论；与类比数据一致。
   - **output**：简洁、准确的最终答案，体现类比对应关系，且事实与上述类比信息一致。

2. 内容需与类比数据、百科信息一致，逻辑自洽，表达清晰。

请直接输出一个 JSON，格式如下：
{{"input": "...", "thinking": "...", "output": "..."}}
